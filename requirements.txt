# Base requirements - every baseline and demo runs on just these.
# CPU-only, no GPU, no downloads, no model weights.
numpy>=1.24
opencv-python-headless>=4.8
matplotlib>=3.7
pandas>=2.0            # used by the legacy ROI-motion pipeline (src/, make synthetic)
